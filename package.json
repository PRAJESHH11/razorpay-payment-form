{
  "name": "razorpay-mern-payment-gateway",
  "version": "1.0.0",
  "description": "MERN Stack Payment Gateway with Razorpay Integration",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "cd backend && npm install && npm start"
  },
  "engines": {
    "node": "18.x"
  },
  "dependencies": {
    "concurrently": "^7.6.0"
  },
  "author": "Your Name",
  "license": "MIT"
}
